{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["generate.mjs"],
      "outputs": ["openapi.json"],
      "dependsOn": ["^build"]
    }
  }
}
